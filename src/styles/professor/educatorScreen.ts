import { theme } from '@/constants/theme';
import { StyleSheet } from 'react-native';

export const educatorScreenStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: theme.bgSubtle,
    },
    content: {
        flex: 1,
    },
});